<h2>Available Resorts</h2>

<% resorts.forEach(r => { %>
  <div>
    <h3><%= r.name %></h3>
    <p><%= r.description %></p>
    <p>Price: ₹<%= r.price %></p>
    <% if (r.image_url) { %>
      <img src="<%= r.image_url %>" width="200">
    <% } %>
    <hr>
  </div>
<% }) %>

<a href="/dashboard">Back</a>
