<!DOCTYPE html>
<html>
<head>
  <title>Admin – View Resorts</title>
</head>
<body>

<h2>Welcome, <%= username %></h2>

<a href="/admin/resorts/add">➕ Add Resort</a>
<br><br>

<table border="1">
  <tr>
    <th>Name</th>
    <th>Price</th>
    <th>Image</th>
    <th>Action</th>
  </tr>

  <% resorts.forEach(resort => { %>
    <tr>
      <td><%= resort.name %></td>
      <td>₹<%= resort.price %></td>
      <td>
        <img src="<%= resort.image_url %>" width="120">
      </td>
      <td>
        <a href="/admin/resorts/delete/<%= resort.id %>">❌ Delete</a>
      </td>
    </tr>
  <% }) %>
</table>

<br>
<a href="/admin/dashboard">⬅ Back</a>

</body>
</html>
